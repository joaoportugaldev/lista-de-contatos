class Contact {
  name: string
  tel: string
  email: string
  id: number

  constructor(name: string, tel: string, email: string, id: number) {
    this.name = name
    this.tel = tel
    this.email = email
    this.id = id
  }
}

export default Contact
